// next.config.js

const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['cdn-icons-png.flaticon.com', 'cdn.icon-icons.com', 'media.licdn.com'], // Adicione seu domínio aqui
  },
};

module.exports = nextConfig;
